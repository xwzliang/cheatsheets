You need to install some system dependencies before installing the Guest Additions

for 18.04：
sudo apt-get install -y gcc make perl

for 20.04:
sudo apt install build-essentail dkms linux-headers-$(uname -r)
